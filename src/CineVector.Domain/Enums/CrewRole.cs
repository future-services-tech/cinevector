namespace CineVector.Domain.Enums;

/// <summary>Ruoli di produzione diversi da regista e cast, utili per il collegamento tematico dei film (es. compositori ricorrenti).</summary>
public enum CrewRole
{
    Composer,
    Writer,
    Producer
}
